Download, unpack and copy PDCurses completely here:
https://sourceforge.net/projects/pdcurses/files/pdcurses/3.6/

So that this path is valid, for example:
SENTRY_ROOT/thirdparty/PDCurses/curses.h

Then build the library, so that x11 or win32 directories with library is available too.